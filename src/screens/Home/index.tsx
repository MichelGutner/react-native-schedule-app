import React from 'react';
import { HomeContainer } from './styles';
import { SearchBar } from '@/components';

export const Home = () => {
  return (
    <HomeContainer>
      <SearchBar />
    </HomeContainer>
  );
};
